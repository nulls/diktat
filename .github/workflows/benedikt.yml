name: Benedikt

on:
  workflow_dispatch:

jobs:
  diktat_check:
    name: 'diKTat Check'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: nulls/benedikt@d8d6c391b8676cb72ebb48548dd99c4e2bf1eabb
        with:
          input-paths: |
            examples/**/*.kt
