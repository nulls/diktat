name: Benedikt

on:
  workflow_dispatch:
  pull_request:

jobs:
  diktat_check:
    name: 'diKTat Check'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: nulls/benedikt@28ca4602b836b946ebf7e10af7e16c7995c884ba
        with:
          reporter: 'checkstyle'
          debug: true
          input-paths: |
            examples/**/*.kt
