name: Benedikt

on:
  workflow_dispatch:
  pull_request:

jobs:
  diktat_check:
    name: 'diKTat Check'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: nulls/benedikt@f30f015b776cda70c6492a192632d666e868b879
        with:
          reporter: 'checkstyle'
          debug: true
          input-paths: |
            examples/**/*.kt
