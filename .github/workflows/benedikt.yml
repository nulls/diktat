name: Benedikt

on:
  workflow_dispatch:
  pull_request:

jobs:
  diktat_check:
    name: 'diKTat Check'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: nulls/benedikt@9ed27ed0e4e6dd39f8756d5ab694f4abda6b542f
        with:
          reporter: 'checkstyle'
          debug: true
          input-paths: |
            examples/**/*.kt
