name: Benedikt

on:
  workflow_dispatch:

jobs:
  diktat_check:
    name: 'diKTat Check'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: nulls/benedikt@ae51fa826c80988379d16a497d648bf9f8e27808
        with:
          input-paths: |
            examples/**/*.kt
