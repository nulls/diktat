name: Benedikt

on:
  workflow_dispatch:
  pull_request:

jobs:
  diktat_check:
    name: 'diKTat Check'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: nulls/benedikt@ff3fedd600793a3a40853fecd577feb7e847f204
        with:
          debug: true
          input-paths: |
            examples/**/*.kt
