name: Benedikt

on:
  workflow_dispatch:
  pull_request:

jobs:
  diktat_check:
    name: 'diKTat Check'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: nulls/benedikt@9f6eaf3beecb4519d90def3a43053df9e84221cc
        with:
          debug: true
          input-paths: |
            examples/**/*.kt
