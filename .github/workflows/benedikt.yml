name: Benedikt

on:
  workflow_dispatch:

jobs:
  diktat_check:
    name: 'diKTat Check'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: nulls/benedikt@a76632e12ce0ef5a5cbef098ef5e869b83067953
        with:
          input-paths: |
            examples/**/*.kt
