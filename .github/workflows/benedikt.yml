name: Benedikt

on:
  workflow_dispatch:
  pull_request:

jobs:
  diktat_check:
    name: 'diKTat Check'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: nulls/benedikt@7ea1e03f681760f2aaf3662ebcee4d39329e474c
        with:
          reporter: test
          input-paths: |
            examples/**/*.kt
