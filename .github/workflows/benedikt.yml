name: Benedikt

on:
  workflow_dispatch:
  pull_request:

jobs:
  diktat_check:
    name: 'diKTat Check'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: nulls/benedikt@9c79f3ae03d722e399112bd1bb16450e830814ff
        with:
          debug: true
          input-paths: |
            examples/**/*.kt
