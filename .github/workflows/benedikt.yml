name: Benedikt

on:
  workflow_dispatch:
  pull_request:

jobs:
  diktat_check:
    name: 'diKTat Check'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: nulls/benedikt@81655a761852d3391352d429a2d1d74618c69b99
        with:
          reporter: 'checkstyle'
          debug: true
          input-paths: |
            examples/**/*.kt
