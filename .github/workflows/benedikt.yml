name: Benedikt

on:
  workflow_dispatch:
  pull_request:

jobs:
  diktat_check:
    name: 'diKTat Check'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: nulls/benedikt@874fc7cdd778a26780e9f2da564da2d62c463920
        with:
          reporter: 'checkstyle'
          debug: true
          input-paths: |
            examples/**/*.kt
