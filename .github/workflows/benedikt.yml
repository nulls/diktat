name: Benedikt

on:
  workflow_dispatch:
  pull_request:

jobs:
  diktat_check:
    name: 'diKTat Check'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: nulls/benedikt@7e985b1e841af0bd9b8a4dcfe1d6b1643c3f5ad3
        with:
          reporter: 'checkstyle'
          debug: true
          input-paths: |
            examples/**/*.kt
