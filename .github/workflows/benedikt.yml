name: Benedikt

on:
  workflow_dispatch:

jobs:
  diktat_check:
    name: 'diKTat Check'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: nulls/benedikt@26e3f27a9eaad7ef47df9e5d7defafbb9c5b85ec
        with:
          input-paths: |
            examples/**/*.kt
