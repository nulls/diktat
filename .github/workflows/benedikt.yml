name: Benedikt

on:
  workflow_dispatch:
  pull_request:

jobs:
  diktat_check:
    name: 'diKTat Check'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: nulls/benedikt@0fa226511b8491bdb9a71408473b8aee29524ab7
        with:
          debug: true
          input-paths: |
            examples/**/*.kt
