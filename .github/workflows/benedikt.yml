name: Benedikt

on:
  workflow_dispatch:

jobs:
  diktat_check:
    name: 'diKTat Check'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: nulls/benedikt@b593be3680c91bd7bf125fbd5cfc90c8973304c3
        with:
          input-paths: |
            examples/**/*.kt
